{
  "private": true,
  "name": "brepkit-workspace",
  "description": "Development tooling for brepkit monorepo",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.1",
    "@commitlint/config-conventional": "^21.0.1",
    "husky": "^9.0.0",
    "prettier": "^3.8.3"
  }
}
